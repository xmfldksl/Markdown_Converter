# PDF_to_Markdown_Converter
This application converts PDF documents into Markdown format while preserving text structure and table layouts. It supports detection of nested tables and automatically reorganizes them into readable Markdown output.
